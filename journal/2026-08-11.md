# Day 0 — 2026-08-11

## What happened
- **Accepted RULES.md v1.3.0** — committed as `a203f8c`.
- **WSL2 + Ubuntu 26.04 LTS installed** — user `jeeni` (uid 1000, sudo). Fix: first-run user prompt needs a real terminal (killed stuck background install, ran `wsl` interactively).
- **AWS account hardened:** IAM user `visakan-admin` + `Admins` group (AdministratorAccess) + U2F MFA + access keys. AWS CLI v2.36.17 installed + configured.
- **Billing alarm chain built:** billing alerts enabled (root console) → SNS topic `cost-alerts` → email subscription confirmed → CloudWatch alarm `billing-alarm-5usd` (EstimatedCharges > $5, us-east-1) → test email received ✓
- **gh CLI authenticated** as `Visakan-official` (device flow via API — PTY Enter was stuck).
- **Anki:** deck `mira::0-foundations/day0` seeded with 7 cards.
- **Obsidian:** Day 0 note written + wikilinked.

## What I ran
- `wsl.exe --status` / `wsl -l -v` → WSL2, Ubuntu running
- `aws sts get-caller-identity` → visakan-admin @ 139316821068
- `aws iam list-groups-for-user` / `list-mfa-devices` / `list-access-keys` → Admins group, U2F MFA, 1 active key
- `aws ec2 describe-instances` (all regions) → t3.small `i-0c0318616efc8d8a0` running in ap-south-2
- `aws billing get-billing-preferences --features BILLING_ALERTS` → ENABLED
- `aws sns create-topic` / `subscribe` / `publish` (test email ✓)
- `aws cloudwatch put-metric-alarm --alarm-name billing-alarm-5usd`
- `gh api user` → Visakan-official
- Dashboard: 0/144 topics

## What broke / fixed
1. WSL install "Provisioning" hang 48 min → interactive OOBE required; killed, reran `wsl` in student terminal.
2. Accidental IAM tag (access key ID as tag key) → `aws iam untag-user`.
3. gh device flow stuck at "Press Enter" in PTY → drove OAuth device flow via curl API, `gh auth login --with-token`.
4. Billing CLI AccessDenied → root activated IAM access (URL `billing/home?#/account` — `?` matters).
5. **Constraint registered:** t3.small (Hermes/Telegram) = PRODUCTION, never touch. (Also in Mira's persistent memory.)

## Questions I have
- When do the 7 Day-0 Anki cards come due? (They're new — first review will be next session per Anki scheduling.)
- Free tier: account says ~115 days left + $160 credits — need to confirm exact expiry date in console next session.
