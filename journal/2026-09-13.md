# 2026-09-13 — Host migration: Windows → Omarchy (Arch) + study-pipeline re-verification

> **Session type:** maintenance / environment migration (not a curriculum topic).
> Phase 0 Anki review is still owed for this session — 7 cards due in `mira::0-foundations/day0`.

## Why

Laptop moved off Windows to **Omarchy 4.0.2** (Arch-based Linux). Everything the bootcamp depended on
had to be re-verified on the new OS: Anki + AnkiConnect + Anki MCP, Obsidian vault, the repo, AWS CLI,
gh CLI. Study materials referenced Windows paths (`A:\...`, WSL2, git-bash) and had to be re-pointed.

## Evidence (real commands, real output)

### 1. Host
```
$ cat /etc/os-release | head -3
NAME="Omarchy"
PRETTY_NAME="Omarchy"
ID=omarchy
ID_LIKE=arch
$ whoami → vizack        $ echo $HOME → /home/vizack
```

### 2. Study pipeline (the two things the whole method hangs on)
```
$ curl -s -X POST http://127.0.0.1:8765 -d '{"action":"version","version":6}'
{"result": 6, "error": null}                      # AnkiConnect add-on UP

$ curl -s -i http://127.0.0.1:3141 | head -1
HTTP/1.1 406 Not Acceptable                       # Anki MCP UP (SSE transport — 406 is expected)

$ ss -ltnp | grep -E '8765|3141'
LISTEN 127.0.0.1:3141  users:(("anki",pid=5995))  # both served by the SAME Anki process
LISTEN 127.0.0.1:8765  users:(("anki",pid=5995))
```
Anki deck check via the MCP tool `list_decks`:
```
mira                                    review_count 7, total 0
mira::0-foundations/day0                review_count 7, total 7
summary: total_cards 7, new 0, learning 0, review_cards 14   → 7 cards are DUE
```
**Verdict: the SRS half of the method survived the migration intact — no cards lost.**

### 3. Obsidian vault
```
$ ls ~/Documents/Obsidian/Mira Bootcamp/
00 Phase 0 - Foundations.md … 07 Phase 7 - Interview Bootcamp.md  Home.md  IAM.md  Notes/  Templates/
$ ls .obsidian/plugins/
anki-sync-plus
$ diff -rq ~/Documents/Obsidian/"Mira Bootcamp" "/run/media/vizack/Creation/Obsidian/Mira Bootcamp"
(no output → Linux copy is identical to the Windows original)
```

### 4. Repo
```
$ find /run/media/vizack -maxdepth 4 -iname "*Ai-assisted*"
/run/media/vizack/Creation/Ai-assisted-learning            # A: drive survived (read-only mount)
$ cp -a "/run/media/vizack/Creation/Ai-assisted-learning" ~/Ai-assisted-learning
$ cd ~/Ai-assisted-learning && git log --oneline -1
1cc8f5d day0 complete: WSL2+Ubuntu, AWS hardening (IAM/MFA/billing alarm), gh auth, 7 anki cards, obsidian note
```
Copied, **not moved** — the NTFS original is untouched as a fallback. Full git history preserved.

### 5. AWS CLI — the big gap
```
$ aws --version
bash: aws: command not found                       # ← missing after the OS switch
$ curl -sS https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip -o /tmp/awscliv2.zip   # 71M
$ /tmp/aws/install -i ~/.local/aws-cli -b ~/.local/bin --update     # no sudo needed
$ aws --version
aws-cli/2.36.44 Python/3.14.6 Linux/7.1.9-arch1-2 exe/x86_64.omarchy.4
$ aws sts get-caller-identity
{"UserId":"AIDASA37N7RGLZXWVLS6U","Account":"139316821068","Arn":"arn:aws:iam::139316821068:user/visakan-admin"}
```
Credentials migrated from the Windows profile (`~/.aws/config` + `credentials`, `chmod 700/600`).
**Identity verified — IAM hardening from Day 0 is still in force (no root).** Region: `ap-south-2`.

### 6. Dashboard script — was hardcoded to Windows
`scripts/build_dashboard.py` had `ROOT_PATH = r"A:\Ai-assisted-learning"` and needed `pip install requests`
(which now **fails** on Arch — PEP 668 externally-managed environment).
Fixed → **v1.4.0**: `ROOT_PATH` auto-derived from the script's own location, Anki HTTP calls rewritten with
`urllib` (stdlib only, zero deps).
```
$ python3 scripts/build_dashboard.py
✅ Dashboard built at /home/vizack/Ai-assisted-learning/docs/index.html (0/144 topics, 0%)
$ grep -o 'Anki: [^<]*' docs/index.html
Anki: 🟢 live                                       # live AnkiConnect stats flowing
$ grep -o 'Due Today</h3><div class="value">[0-9]*' docs/index.html
Due Today</h3><div class="value">7
```

### 7. Documents de-Windowsified (v1.3.0 → v1.4.0)
| File | What changed |
|---|---|
| `RULES.md` | Rule 5 lab host WSL2 → Omarchy/Arch native; Rule 11 vault path → `~/Documents/Obsidian/Mira Bootcamp`; Rule 14 `python scripts\…` → `python3 scripts/…` |
| `README.md` | Lab-host block added; vault path; version v1.4.0 |
| `RESOURCES.md` | Tool timeline: WSL2/Docker Desktop → native Arch; **new "Linux host notes — Omarchy/Arch" table** (pacman, AWS CLI bundle recipe, PEP 668 warning, Anki endpoint check) |
| `ROADMAP.md` | M0.1 "Package management (apt)" → **pacman/AUR** (apt kept as interview concept) |
| `meta/mira-devops-teacher-SKILL.md` | Synced to teacher protocol **v1.4.0** |

## What broke / what I fixed
| Break | Fix |
|---|---|
| `aws` not found on Linux | Official AWS CLI v2 bundle → `~/.local/aws-cli` + `~/.local/bin` symlink (no sudo) |
| `~/.aws` missing | Migrated config + credentials from Windows profile; `chmod 700 ~/.aws`, `chmod 600` files |
| Dashboard script Windows-hardcoded | `ROOT_PATH` from `__file__`; `requests` → stdlib `urllib` |
| `pip install requests` blocked | Avoided entirely — script is now dependency-free (PEP 668) |
| Docs pointing at `A:\` + WSL2 + git-bash | All live docs re-pointed; historical journal entries left as-is (they're true history) |

## Open items (student's turn)
1. **`gh auth login`** — gh 2.99.0 is installed but unauthenticated → no push possible until this is done.
   Device flow (GitHub shows a code, HE types/confirms it in the browser). I never ask for the token.
2. **Phase 0 Anki review** — 7 cards due in `mira::0-foundations/day0` (Day-0 cards: IAM/MFA, billing
   alarm, free tier, root vs IAM user, production constraint…). Owed before any new material.
3. **Push this commit** from the Linux host → verifies end-to-end pipeline on the new OS.

## Next session
`M0.1 — Linux Fundamentals`: filesystem layout, navigation, file ops (`ls`, `find`, `cp`, `mv`, `ln`),
then permissions & ownership. Deck: `mira::0-foundations/linux`.
